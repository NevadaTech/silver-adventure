# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# nest / generic builds
dist/
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# local MCP config (contains personal API keys — share via .example only)
.cursor/mcp.json
!.cursor/mcp.json.example

# Playwright MCP scratch (screenshots, snapshots, console logs)
.playwright-mcp/
landing-*.png

# vercel
.vercel

# supabase CLI local state (credentials, project ref)
supabase/.temp/
supabase/.branches/

# typescript
*.tsbuildinfo
next-env.d.ts
